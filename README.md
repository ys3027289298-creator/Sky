# 星陨防线

《星陨防线》是一个可直接在浏览器运行的原创 3D 太空射击游戏，使用 TypeScript、Vite、Three.js 和 Vitest 开发。玩家驾驶轻型战斗飞船，在废弃采矿区、失控空间站和敌方舰队外围完成连续任务，管理燃料、弹药、护盾、装甲、能量、维修材料与改装取舍。

## 启动

```powershell
npm install
npm run dev
```

打开终端中显示的本地地址，通常是 `http://localhost:5173/`。无需注册、登录或后端服务。

生产构建：

```powershell
npm run build
npm run preview
```

## 测试

```powershell
npm test
```

测试覆盖：

- 飞船推进、惯性、加速限制、碰撞伤害
- 武器射速、弹药、过热、冷却和命中统计
- 导弹锁定、遮挡脱锁、目标切换
- 护盾、装甲、部位损伤与维修
- 敌人巡逻、发现、追击、攻击、撤退和死亡
- 四个任务的开始、完成、失败和撤离
- 航点阶段切换、等待/空状态、距离与四方向判断、面板投影夹紧和 NaN 防护
- 页面级导航 HUD：开始任务显示航点、推进步骤后文本和距离同步、N 键面板、暂停冻结、重开清理
- 资源拾取、三级改装、localStorage 存档
- 六种动态事件和指挥舰护盾/弱点战斗

## 操作

| 操作 | 按键 |
| --- | --- |
| 加速 / 减速 | `W` / `S` |
| 横向移动 | `A` / `D` |
| 朝向 | 鼠标移动 |
| 侧向翻滚 | `Q` / `E` |
| 主武器 | 鼠标左键 |
| 导弹 / 爆破弹 | 鼠标右键 |
| 切换四种武器 | `1` 双联、`2` 脉冲、`3` 导弹、`4` 爆破 |
| 加力冲刺 | `Shift` |
| 紧急闪避 | `空格` |
| 武器冷却 | `R` |
| 任务和装备信息 | `Tab` |
| 展开 / 收起战术导航面板 | `N` |
| 追尾 / 驾驶舱视角 | `C` |
| 暂停、继续、重开 | `Esc` |

任务交互点靠近后按 `F`：侦察扫描、空间站防御节点、终战干扰塔均使用该交互。

## 航点导航

HUD 顶部中央的导航卡实时显示当前航点的名称、类型、距离、相对方向和状态（前往中 / 已到达 / 等待条件），不遮挡准星和雷达。按 `N` 展开战术导航面板，以飞船为原点简化投影当前航点、已知敌人和补给；超出面板半径的目标会被稳定夹在边缘并保留方向角。

- **航点类型**：信标、扫描区、撤离点、会合点、护航点、空间站、能源核心、干扰塔、指挥舰。每个航点包含 id、名称、类型、世界坐标、任务序号、目标步骤、激活条件和完成状态，由 `MissionManager` 统一返回当前航点与后续航点。
- **方向计算**：把目标偏移投影到飞船的前向 / 右向向量（由 yaw 旋转得到），取绝对值较大的分量判定前方、后方、左侧、右侧；非有限输入回退为前方，距离为 0，不会产生 NaN。
- **四任务导航规则**：
  1. 采矿区：采矿区信标 → 扫描区 → 撤离点（扫描完成后撤离开启）。
  2. 护航：运输船会合点 → 护送撤离点（清除截击力量后切换）。
  3. 空间站：防御节点 → 能源核心 → 爆炸倒计时内的撤离点。
  4. 终战：干扰塔 α → 干扰塔 β → 指挥舰（位置实时跟随）→ 终局撤离点。
- 任务成功或失败后导航自动隐藏；暂停时距离冻结；重新开始任务会清理上一局航点并收起面板。

## 推荐 10–15 分钟录屏路线

1. 主菜单选择“简单练习”或“完整流程”，第一关飞入左前方废弃采矿区。
2. 靠近扫描区域按 `F`，展示无人机、陨石碰撞和撤离点。
3. 完成第一关后回到主菜单，进入“飞船改装”，升级引擎、护盾或武器。
4. 开始后续任务，途中展示左键主炮过热、右键导弹锁定、补给拾取和维修。
5. 空间站任务摧毁三个防御节点、取得核心，在倒计时结束前撤出。
6. 最终任务关闭两座干扰塔，再攻击指挥舰弱点，最后穿过舰队火力撤离。
7. 可主动撞击陨石或不恢复护盾录制失败页；失败原因会明确显示。

## 任务

1. **废弃采矿区侦察**：进入采矿区、扫描目标、躲避无人机并返航。
2. **陨石带护航**：与运输船路线会合，消灭截击力量，运输船生命归零则失败。
3. **失控空间站突袭**：摧毁三个防御节点，取得能源核心，在爆炸倒计时前撤离。
4. **舰队外围终战**：关闭两座干扰塔，击破带护盾和弱点的指挥舰，在合围前撤离。

## 武器与防御

- **双联能量炮**：均衡主炮，持续射击会过热。
- **高速脉冲炮**：高射速、短射程，适合截击机。
- **追踪导弹**：需要锁定，有限弹药，可被遮挡和距离影响。
- **范围爆破弹**：慢速高伤，适合重型目标和集群。

飞船拥有生命、护盾、装甲、能量、燃料和部位耐久。护盾优先承伤并在脱战后恢复；装甲和引擎、武器、雷达损伤不会完全自愈，需要寻找维修信标或消耗维修包。

## 动态事件

游戏包含陨石带偏移、敌人增援、护盾故障、能源站爆炸、运输船改线和空间风暴。事件有倒计时提示，并实际改变增援、航线、能见度、护盾恢复、能量消耗或战斗压力。

## 存档和改装

游戏使用浏览器 `localStorage` 保存：

- 已解锁任务
- 资源和改装点
- 六类装备的三级升级
- 鼠标灵敏度和音量

改装不是纯数字：引擎影响速度和冲刺，护盾影响护盾量和回复，装甲影响减伤，武器影响伤害和射速，雷达影响锁定距离，导弹舱增加副武器弹药。

## 项目结构

```text
index.html
package.json
vite.config.ts
tsconfig.json
src/
  main.ts                  入口
  styles.css               HUD、菜单和结算样式
  core/
    types.ts               向量和通用类型
    ship.ts                飞船惯性、资源、部位、碰撞和维修
    weapons.ts             四种武器、过热、弹药和锁定逻辑
    enemies.ts             五种敌人与 AI 状态机
    missions.ts            四关任务条件和胜负判定
    events.ts              动态事件和环境效果
    waypoints.ts           航点数据、方向/距离、面板投影和开关状态（纯逻辑）
    storage.ts             存档和改装
  game/
    GameApp.ts             输入、游戏流程、HUD、雷达和胜负
    world.ts               Three.js 场景、区域、障碍、敌人、弹道和粒子
    audio.ts               WebAudio 程序化占位音效
tests/
  core.test.ts             Vitest 核心逻辑测试
  waypoints.test.ts        航点选择、方向、投影和面板状态测试
  nav-page.test.ts         页面级导航 HUD 测试
```

所有模型和材质均由 Three.js 基础几何体与程序化材质生成，音效由 WebAudio 合成，不包含外部商业游戏素材。
